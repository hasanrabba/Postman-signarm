node_modules
.next
out
.env
.env.local
.env.*.local
*.tsbuildinfo
next-env.d.ts
.DS_Store
src-tauri/target
src-tauri/gen/schemas
.api.stash
launcher/target
launcher/payload/*.exe
launcher/payload/*.dll
installer/staging
installer/SignarmSignal-Setup.exe

# QA sweep scratch. Subagents write throwaway probes and counter-tests here
# under whatever name they pick, so the convention is the leading underscore:
# anything in tests/ starting with one is scratch and never committed.
tests/_*

# snippet execution harness scratch (cleaned up by the test, ignored if it dies)
.snip-*
.snipd-*
