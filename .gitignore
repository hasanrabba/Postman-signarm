node_modules
.next
out
.env
.env.local
.env.*.local
*.tsbuildinfo
next-env.d.ts
.DS_Store
src-tauri/target
src-tauri/gen/schemas
.api.stash
launcher/target
launcher/payload/*.exe
launcher/payload/*.dll
installer/staging
installer/SignarmSignal-Setup.exe

# QA sweep scratch: subagents write throwaway probes here. Both prefixes —
# _probe_ for a finder's own repro, _refute_ for a verifier's counter-test.
tests/_probe_*
tests/_refute_*

# snippet execution harness scratch (cleaned up by the test, ignored if it dies)
.snip-*
.snipd-*
