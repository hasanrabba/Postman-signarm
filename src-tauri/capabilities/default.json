{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allowlist for the Signarm Signal main window. Only the commands used by the frontend are permitted; everything else on the Tauri IPC surface is refused so a compromised page cannot escalate into the host.",
  "windows": ["main"],
  "permissions": [
    "core:app:default",
    "core:window:default",
    "core:webview:default",
    "core:event:default",
    "core:path:default",
    "core:resources:default"
  ]
}
